{
  "name": "@vetlog/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"api dev server not yet implemented\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet\"",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:validate": "prisma validate"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "@vetlog/shared": "workspace:*",
    "prisma": "^7.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "dotenv": "^17.4.2"
  }
}
